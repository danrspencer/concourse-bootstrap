jobs:
- name: hello-world
  plan:
  - get: bootstrap
    trigger: true
  - task: hello-world
    file: hello-world/ci/tasks/hello.yml
    params:
      who: world

resources:
- name: bootstrap
  type: git
  source:
    uri: {{git-repo}}
    branches: master
    private_key: {{private-repo-key}}